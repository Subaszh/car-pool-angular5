import { AddEmptyClassDirective } from './add-empty-class.directive';

describe('AddEmptyClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AddEmptyClassDirective();
    expect(directive).toBeTruthy();
  });
});
